import styled from "styled-components";

type IProps = {

}

function Project(props: IProps) {
    const {  } = props;

    return (
        <div>
            
        </div>
    );
}

export default Project;